import { useState } from "react"
import { StyledCartModal } from "./style";

export const CartModal = ()=>{
    const [ setIsOpenModal, isOpenModal ]=useState(false);
    return (
        <StyledCartModal>

        </StyledCartModal>
    )
}